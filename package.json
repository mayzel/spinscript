{
  "name": "spinscript",
  "displayName": "SpinScript",
  "description": "Bruker TopSpin Pulse Programming Language Support",
  "version": "0.0.9",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mayzel/spinscript"
  },
  "engines": {
    "vscode": "^1.106.1"
  },
  "categories": [
    "Programming Languages"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onLanguage:spinscript"
  ],
  "contributes": {
    "languages": [
      {
        "id": "spinscript",
        "aliases": [
          "spinscript","SpinScript","Bruker"
        ],
        "extensions": [
          "",
          ".incl"        
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "spinscript",
        "scopeName": "source.spinscript",
        "path": "./syntaxes/spinscript.tmLanguage.json"
      }
    ],
    "configuration": [ {
      "title": "SpinScript",
      "properties": {
        "spinscript.pulseProgramPaths": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Directories containing pulse program files to recognize as SpinScript (e.g., ${workspaceFolder}/pp, $TSHOME/exp/stan/nmr/lists/pp)"
        },
        "spinscript.autoFileAssociations": {
          "type": "object",
          "description": "File associations for SpinScript files automatically detected by the extension. This is managed by the extension and should not be edited manually.",
          "scope": "window",
          "default": {}
        }
      }
    } ],
    "configurationDefaults": {}
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/vscode": "^1.106.1",
    "typescript": "^5.9.3"
  }
}
